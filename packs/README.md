# Packs

Each folder is one Steam language:

```
packs/<id>/info.txt     ← menu name (first line)
packs/<id>/dialogs/
packs/<id>/knowledge/
```

Install copies `packs/<id>` → `Underrail/data/localization/<id>`.
